# Code Revisions

This folder currently contains `Systems engineering code.lnk`, which is a Windows shortcut rather than portable source code.

GitHub cannot use a `.lnk` file as reviewable implementation evidence because it points to a local path outside the repository. If the original code becomes available, replace the shortcut with the real source file and update this note.
